import { Component, Input } from '@angular/core';

@Component({
    selector: 'other',
    template: `<div>
        <toggle-on>Other Toggle on</toggle-on>
        <toggle-off>Other Toggle off</toggle-off>
    </div>`
})
export class OtherComponent {
    
}